# Digital Marketing Portfolio

React + Vite 的个人代运营作品集基础版本。

## 本地预览

```bash
pnpm install
pnpm dev
```

然后打开终端显示的本地地址。生产构建使用：

```bash
pnpm build
```

## 可继续调整的内容

- `public/videos/` 已放入 6 条精选作品；在 `src/main.jsx` 的 `works` 数组中可更新作品标题、分类与说明。
- 首屏主视觉位于 `public/images/hero-orbit.png`，为本项目生成的原创图片。
